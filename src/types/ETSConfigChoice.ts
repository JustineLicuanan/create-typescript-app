export enum ETSConfigChoice {
	NODE = 'for Node',
	NEXT_JS = 'for Next.js',
	REACT = 'for React',
}

export enum ETSConfigFileName {
	NODE = 'tsconfig.node.json',
	NEXT_JS = 'tsconfig.next.json',
	REACT = 'tsconfig.react.json',
}
